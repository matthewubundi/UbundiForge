# Reference Docs

This directory contains captured upstream material and local help snapshots used while shaping Forge prompts and backend integration behavior.

These files are intentionally useful but non-authoritative:

- CLI help snapshots can drift as external tools change flags or defaults.
- Provider prompt guides can drift as models, APIs, and recommendations change.

Before turning a reference snippet into product behavior or user-facing documentation, verify it against the current upstream source.
